{
  "name": "slapp-evaluation-function",
  "version": "1.0.0",
  "description": "Google Cloud Function for processing evaluation tasks with Gemini",
  "main": "index.js",
  "scripts": {
    "start": "functions-framework --target=processEvaluation",
    "deploy": "gcloud functions deploy processEvaluation --runtime nodejs20 --trigger-http --allow-unauthenticated --region asia-south1 --memory 512MB --timeout 540s"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.3.0",
    "@google-cloud/tasks": "^6.2.1",
    "@google/generative-ai": "^0.21.0"
  },
  "engines": {
    "node": ">=18"
  }
}

