{
  "name": "slapp-cloud-functions",
  "version": "1.0.0",
  "description": "Google Cloud Functions for Slapp evaluation processing with Gemini and MongoDB",
  "main": "index.js",
  "scripts": {
    "deploy": "gcloud functions deploy processEvaluation --gen2 --runtime nodejs20 --trigger-http --allow-unauthenticated --region asia-south1 --memory 1GB --timeout 540s --env-vars-file=.env.yaml --project=slapp-478005",
    "logs": "gcloud functions logs read processEvaluation --region=asia-south1 --project=slapp-478005 --limit=100",
    "logs:follow": "gcloud functions logs read processEvaluation --region=asia-south1 --project=slapp-478005 --follow"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.3.0",
    "@google-cloud/tasks": "^6.2.1",
    "@google-cloud/vertexai": "^1.9.0",
    "mongoose": "^8.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "js-yaml": "^4.1.1"
  }
}
